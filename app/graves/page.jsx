import { Suspense } from "react";
import Link from "next/link";
import GravesList from "./GravesList";
import Loading from "../(dashboard)/loading";

export const metadata = {
  title: "GraveFinder | Graves",
  description: "Generated by create next app",
};

export default function Graves() {
  return (
    <main>
      <nav>
        <div>
          <h2>Graves</h2>
          <p>
            <small>Available graves to locate.</small>
          </p>
        </div>
        <Link href={"/graves/create"} className="ml-auto">
          <button className="btn-primary">Add a new Grave</button>
        </Link>
      </nav>

      <Suspense fallback={<Loading />}>
        <GravesList />
      </Suspense>
    </main>
  );
}
